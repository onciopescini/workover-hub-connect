/**
 * @deprecated Redirect orchestration has been centralized in AuthFlowGate.
 * Keep this component as a compatibility no-op until all references are removed.
 */
export const AuthRedirector = () => {
  return null;
};
